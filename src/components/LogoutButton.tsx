// Este arquivo manteve-se por compatibilidade. Use os novos componentes separados:
// - `ProfileHeader` (foto + nome)
// - `SignOutButton` (botão Sair + confirmação)

export { default as ProfileHeader } from './ProfileHeader';
export { default as SignOutButton } from './SignOutButton';

